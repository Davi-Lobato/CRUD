#include <stdio.h>
#include <stdlib.h>

class orientador
{
    public:
    // INFO ORIENTADOR
    char orientador_nome[50]; // Preenchimento obrigatório
} ;

void adicionar_orientador(orientador);

class novo_orientador
{
    public:
    char novo_orientador[50];
};